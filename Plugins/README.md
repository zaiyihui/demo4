# Plugins

## 职责
定义和管理可扩展插件的元数据、加载和生命周期入口。

## 关键文件

| 文件 | 职责 |
|---|---|
| `IPlugin.cs` | 插件契约 |
| `PluginAttribute.cs` | 插件标记和元数据 |
| `PluginManager.cs` | 插件发现、加载和管理 |

## 注意事项
插件属于进程内扩展，加载第三方程序集前应验证路径、版本和异常边界；不要让插件阻塞监控或 UI 线程。