# scripts

## 职责
提供 Windows PowerShell 维护和性能辅助脚本。

## 关键文件

| 文件 | 职责 |
|---|---|
| `benchmark.ps1` | 生成本机性能基准数据 |
| `内存优化.ps1` | 监控或处理构建内存占用 |
| `清理构建.ps1` | 清理构建缓存和临时进程 |

## 注意事项
脚本只面向 Windows PowerShell；执行前确认路径和权限，清理脚本会删除构建缓存。