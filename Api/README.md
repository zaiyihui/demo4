# Api

## 职责
定义应用内部 API 请求、响应和控制器入口，承载面向功能模块的数据传输对象。

## 关键文件

| 文件 | 职责 |
|---|---|
| `ApiController.cs` | API 调用协调入口 |
| `ApiRequest.cs` | 请求模型 |
| `ApiResponse.cs` | 响应模型 |

## 注意事项
保持请求/响应模型与调用方兼容；不要在 DTO 中直接放入硬件句柄或 UI 状态。