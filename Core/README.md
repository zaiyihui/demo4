# Core

## 职责
承载跨界面复用的抽象、核心模型和通用服务，例如日志、备份、本地规则洞察、性能统计和插件服务。

## 关键目录

| 路径 | 职责 |
|---|---|
| `Abstractions/` | 核心服务接口 |
| `Models/` | 核心数据模型 |
| `Services/` | 核心服务实现 |

## 注意事项
`InsightService` 仅提供本地规则洞察；核心服务通过接口注入，修改接口时同步更新测试。